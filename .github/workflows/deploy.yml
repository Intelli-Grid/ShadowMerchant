name: Deploy to Production

# NOTE: Vercel auto-deploys the web app on every push to master via Git integration.
# This file is kept as a placeholder for future separate services (e.g., a background API).
# The deploy-api job was removed — apps/api does not exist in this repo.
# If a Render-hosted API service is added in the future, restore that job here.

on:
  push:
    branches: [master]

# No jobs currently — deployments are handled by Vercel and GitHub Actions scrape.yml
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Web deployment handled by Vercel Git integration. No manual deploy needed."

